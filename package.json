{
  "name": "polara",
  "private": true,
  "version": "0.1.0",
  "description": "POLARA — Ride the Northern Lights. Tek parmakla oynanan hipnotik bir kuzey ışığı oyunu.",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "cap:sync": "npm run build && cap sync",
    "cap:android": "npm run build && cap sync android && cap open android",
    "cap:ios": "npm run build && cap sync ios && cap open ios"
  },
  "devDependencies": {
    "vite": "^5.4.0"
  },
  "dependencies": {
    "@capacitor/android": "^6.2.0",
    "@capacitor/cli": "^6.2.0",
    "@capacitor/core": "^6.2.0",
    "@capacitor/ios": "^6.2.0",
    "firebase": "^12.17.0"
  }
}
