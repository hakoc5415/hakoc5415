{
  "name": "ro-viking-raid",
  "version": "1.0.0",
  "description": "RO! Viking Raid — Capacitor native wrapper (Android + iOS) with Firebase world leaderboard backend.",
  "private": true,
  "scripts": {
    "build:web": "esbuild src/leaderboard.js --bundle --minify --format=iife --outfile=www/js/leaderboard.js",
    "sync": "npm run build:web && cap sync",
    "android": "cap open android",
    "ios": "cap open ios",
    "functions:deploy": "cd functions && npm run deploy"
  },
  "repository": {
    "type": "git",
    "url": "http://local_proxy@127.0.0.1:41729/git/hakoc5415/hakoc5415"
  },
  "license": "UNLICENSED",
  "dependencies": {
    "@capacitor/android": "^8.4.1",
    "@capacitor/app": "^8.1.0",
    "@capacitor/core": "^8.4.1",
    "@capacitor/haptics": "^8.0.2",
    "@capacitor/ios": "^8.4.1",
    "@capacitor/splash-screen": "^8.0.1",
    "@capacitor/status-bar": "^8.0.2",
    "firebase": "^12.15.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^8.4.1",
    "esbuild": "^0.28.1"
  }
}
